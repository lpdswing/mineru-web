# Worker 配置 - 业务 worker 副本数量
WORKER_REPLICAS=1

# MinIO 配置：填写浏览器和容器都能访问的宿主机 IP/域名
MINIO_ENDPOINT=SERVER_IP:9000

# 仅在使用 vlm-http-client / hybrid-http-client 并接入外部 OpenAI 兼容服务时配置
# SERVER_URL=http://openai-compatible-server:30000
